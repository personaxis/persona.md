# Migration report 0.10 → 1.0

- **Source:** C:\Users\daqc\Documents\GitHub\persona.md\.personaxis\personas\frontend-expert\personaxis.md
- **Timestamp:** 2026-07-03T20:46:24.929Z

## Changes applied
- `apiVersion`: persona.dev/v1 → `personaxis.com/v1`
- `spec_version`: 0.10.0 → `"1.0.0"`
- Dropped `metadata.display_name` (single owner: `identity.display_name`)
- Renamed `reflexive_self_regulation` → `self_regulation` (layer 9 + per_layer_edit_policy + drift_thresholds)
- Merged 2 `principled_refusals` item(s) into `character.prohibited_behaviors`
- Moved `memory.retrieval_policy` knobs → `runtime.memory` (faculty vs implementation split)
- Moved `memory.deletion_policy.retention_days_default` → `runtime.memory`
- Converted 3 drive `intensity` value(s) → static `level` (a drive is mutable ONLY by declaring a {mean, range} envelope)
- Renamed 12 state.json value key(s) to full dot-paths (e.g. `mood.tone` → `affect.baseline.mood.tone`)

## Warnings
_(none)_

## Manual follow-ups required
- OPTIONAL: add `refs:` to hard-enforced virtues pointing at their backing trait/value dot-paths (e.g. honesty → [personality.traits.honesty_humility]); the validator then enforces coherence.
- OPTIONAL: upgrade metacognition monitors from booleans to `{enabled, feeds}` to wire monitor → decision explicitly.
- OPTIONAL: declare behavior `bands` boundaries on traits for deterministic compile semantics (drift = band crossing).

---

Validate the migrated artifact:

```bash
personaxis validate C:\Users\daqc\Documents\GitHub\persona.md\.personaxis\personas\frontend-expert\personaxis.md
```
